- Can have dynamic input element by setting the return jsx to a specific type of component like input or textarea
	- Props can then be spread into the element so that any JSX specific attribues will automatically be  distributed to the component on render
-